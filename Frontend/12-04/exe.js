// Expressão
const abrilExpressao = function() {
    console.log("Abril de 2023 é TOP!!!");
}
abrilExpressao();

// Calculando um retângulo
const calcularRetangulo = function(base, altura) {
    return base * altura;
  }
  console.log(calcularRetangulo(400, 500));

// Calculando a área do círculo
const calcularCirculo = (raio) => {
    return Math.PI * (raio ** 2);
  }
  console.log(calcularCirculo(5));
  